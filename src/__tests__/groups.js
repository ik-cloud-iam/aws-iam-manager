const AWS = require('aws-sdk');
const AWS_MOCK = require('aws-sdk-mock');
const Policies = require('../Policies');

describe('Groups Service', () => {
  describe('#addUserToGroup', () => {
    it('adds user to a group', done => {
      done();
    });
  });
});
